<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Hotwire Training" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <!-- Tailwind (replaces Simple.css) -->
    <script src="https://cdn.tailwindcss.com"></script>

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-50 text-gray-900 min-h-screen flex flex-col">
    <!-- Navbar (Devise-aware) -->
    <nav class="bg-white border-b shadow">
      <div class="max-w-6xl mx-auto px-4 py-3 flex justify-between items-center">
        <%= link_to "Hotwire Training", root_path, class: "text-xl font-bold text-blue-600" %>

        <div class="space-x-4 text-sm">
          <% if user_signed_in? %>
            <span class="text-gray-700">Signed in as <%= current_user.email %></span>
            <%= link_to "Edit profile", edit_user_registration_path, class: "text-gray-700 hover:text-blue-600" %>
            <%= link_to "Logout", destroy_user_session_path, data: { turbo_method: :delete }, class: "text-red-600 hover:text-red-800" %>
          <% else %>
            <%= link_to "Sign up", new_user_registration_path, class: "text-gray-700 hover:text-blue-600" %>
            <%= link_to "Login", new_user_session_path, class: "text-gray-700 hover:text-blue-600" %>
          <% end %>
        </div>
      </div>
    </nav>

    <!-- Flash messages -->
    <div class="max-w-6xl mx-auto px-4 mt-4">
      <% flash.each do |key, value| %>
        <% css = case key.to_sym
          when :notice then "bg-green-100 text-green-800"
          when :alert then "bg-red-100 text-red-800"
          else "bg-gray-100 text-gray-800"
        end %>
        <div class="mb-3 px-4 py-2 rounded-lg <%= css %>"><%= value %></div>
      <% end %>
    </div>

    <!-- Main -->
    <main class="max-w-6xl mx-auto px-4 py-6 flex-1">
      <%= yield %>
    </main>

    <footer class="bg-gray-100 border-t py-4 text-center text-gray-500 text-sm">
      &copy; <%= Time.current.year %> Hotwire Training
    </footer>
  </body>
</html>
